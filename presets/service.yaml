apiVersion: v1
kind: Service
metadata:
  name: #string
  namespace: #string
  annotations:
    # key: value
  labels:
    # key: value
spec:
  type: ClusterIP # ClusterIP, NodePort, LoadBalancer, ExternalName
  selector:
    # key: value
  ports:
    - name: #string
      protocol: TCP
      port: #int
      targetPort: #int
      # nodePort: int
