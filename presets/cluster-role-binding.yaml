apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: #string
  annotations:
    # key: string
  labels:
    # key: string
# roleRef:
#   apiGroup: rbac.authorization.k8s.io
#   kind: ClusterRole
#   name: string
# subjects:
#   - kind: ServiceAccount
#     name: string
#     namespace: string  