apiVersion: aipub.ten1010.io/v1alpha1
kind: AIPubVolume
metadata:
  name: #string
  namespace: #string
  annotations:
    # key: value
  labels:
    # key: value
spec:
  capacity: 1Gi
  storageClassName: #string
